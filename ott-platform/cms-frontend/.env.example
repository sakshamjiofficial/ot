# CMS Frontend environment variables
# These are baked into the build via Vite — set before running npm run build

# API URL (same domain, proxied through Nginx)
VITE_API_URL=/api/v1

# For local development
# VITE_API_URL=http://localhost:3000/api/v1

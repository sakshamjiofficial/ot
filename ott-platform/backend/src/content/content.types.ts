export enum ContentType {
  MOVIE  = 'movie',
  SERIES = 'series',
}

export enum ContentStatus {
  DRAFT      = 'draft',
  PROCESSING = 'processing',
  PUBLISHED  = 'published',
  SCHEDULED  = 'scheduled',
  ARCHIVED   = 'archived',
}
